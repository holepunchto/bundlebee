// This file is autogenerated by the hyperschema compiler
// Schema Version: 1
/* eslint-disable camelcase */
/* eslint-disable quotes */
/* eslint-disable space-before-function-paren */

const { c } = require('hyperschema/runtime')

const VERSION = 1

// eslint-disable-next-line no-unused-vars
let version = VERSION

// @bundlebee/entry
const encoding0 = {
  preencode(state, m) {
    c.buffer.preencode(state, m.source)
    c.stringRecord.preencode(state, m.resolutions)
  },
  encode(state, m) {
    c.buffer.encode(state, m.source)
    c.stringRecord.encode(state, m.resolutions)
  },
  decode(state) {
    const r0 = c.buffer.decode(state)
    const r1 = c.stringRecord.decode(state)

    return {
      source: r0,
      resolutions: r1
    }
  }
}

// @bundlebee/manifest
const encoding1 = {
  preencode(state, m) {
    c.uint.preencode(state, m.abi)
  },
  encode(state, m) {
    c.uint.encode(state, m.abi)
  },
  decode(state) {
    const r0 = c.uint.decode(state)

    return {
      abi: r0
    }
  }
}

function setVersion(v) {
  version = v
}

function encode(name, value, v = VERSION) {
  version = v
  return c.encode(getEncoding(name), value)
}

function decode(name, buffer, v = VERSION) {
  version = v
  return c.decode(getEncoding(name), buffer)
}

function getEnum(name) {
  switch (name) {
    default:
      throw new Error('Enum not found ' + name)
  }
}

function getEncoding(name) {
  switch (name) {
    case '@bundlebee/entry':
      return encoding0
    case '@bundlebee/manifest':
      return encoding1
    default:
      throw new Error('Encoder not found ' + name)
  }
}

function getStruct(name, v = VERSION) {
  const enc = getEncoding(name)
  return {
    preencode(state, m) {
      version = v
      enc.preencode(state, m)
    },
    encode(state, m) {
      version = v
      enc.encode(state, m)
    },
    decode(state) {
      version = v
      return enc.decode(state)
    }
  }
}

const resolveStruct = getStruct // compat

module.exports = {
  resolveStruct,
  getStruct,
  getEnum,
  getEncoding,
  encode,
  decode,
  setVersion,
  version
}
